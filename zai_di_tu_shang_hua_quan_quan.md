# 在地圖上畫圈圈




        // Add circle in 400 meters
        CircleOptions circleOptions = new CircleOptions()
                .center(position)
                .radius(400);

        map.addCircle(circleOptions);
